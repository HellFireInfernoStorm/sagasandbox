/** Re-exports from generated DB types — prefer `@/types/db` for new imports. */
export type {
  Character,
  CharacterRole,
  Export,
  ExportStatus,
  ExportType,
  GenStatus,
  GhostTimelineSuggestion,
  LocationPin,
  Profile,
  Project,
  ProjectInsert,
  ProjectMember,
  StyleConfig,
  TimelineEvent,
  VisualTraits,
} from "./db"

export { asGenStatus, getVisualTraits } from "./db"
